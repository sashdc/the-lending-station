{{!-- renders book tiles for user dashboard and library --}}

<div class="col-lg-3 col-md-4 col-6" id="book-tile">
      <a href="/book/{{id}}" class="d-block mb-4 h-100">
        <img class="img-fluid img-thumbnail" src="/images/covers/{{id}}.jpg" alt="{{title}} cover" style="height:280px;">
          <h5>{{title}}</h5>
      </a>
    </div>

    